// Zod validation schemas for forms and API payloads

export function loginSchema() {
  // Zod schema for login form (email, password)
}

export function registerSchema() {
  // Zod schema for registration form (name, email, password, confirmPassword)
}

export function createAuctionSchema() {
  // Zod schema for creating an auction (title, description, startingPrice, endDate, images)
}

export function placeBidSchema() {
  // Zod schema for placing a bid (amount)
}
